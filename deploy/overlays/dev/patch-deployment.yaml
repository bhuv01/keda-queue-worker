apiVersion: apps/v1
kind: Deployment
metadata:
  name: queue-worker
spec:
  template:
    spec:
      containers:
        - name: worker
          env:
            - name: LOG_LEVEL
              value: "debug"
